CC=gcc
CFLAGS=-Wall -Wextra -O2 -Iinclude
LDFLAGS=

TARGET=digital_lock
SRC=src/main.c src/lock_logic.c src/io_stub.c
OBJ=$(SRC:.c=.o)

all: $(TARGET)

$(TARGET): $(OBJ)
	$(CC) $(OBJ) -o $(TARGET) $(LDFLAGS)

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJ) $(TARGET)

.PHONY: all clean
